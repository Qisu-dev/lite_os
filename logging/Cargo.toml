[package]
name = "logging"
version = "0.1.0"
edition = "2024"

[dependencies]
log = { version = "0.4.29", features = [
    "max_level_debug",
    "release_max_level_warn",
] }
polished_serial_logging = "0.1.2"
